
import 'package:flutter/material.dart';

class MatchesPage extends StatelessWidget {
  const MatchesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Jogos (placeholder)")),
      body: const Center(
        child: Text("Aqui vais ligar a tua página de Jogos/Táticas depois."),
      ),
    );
  }
}
